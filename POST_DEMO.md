# POST_DEMO Backlog (Do Later)

- Add HR role workflow (if still required after feedback).
- Add server-side pagination/filtering for large admin complaint lists.
- Normalize old/legacy auth screens (`/otp`, `/set-password`, `/otp-verify`) into one consistent flow.
- Add automated API integration tests for auth + complaint lifecycle.
- Improve super-admin panel with route-level guard component.
- Add audit logs export and admin activity timeline enhancements.
- UI polish pass (non-blocking animation/spacing/typography refinements).
- Optional: migrate OTP storage to hashed-only for investigator OTP fields.
